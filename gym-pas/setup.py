from setuptools import setup

setup(name='gym_pas',
      version='0.0.1',
      install_requires=['gym']#And any other dependencies required
      # install_requires=['gym', 'random', 'json', 'pandas', 'numpy']#And any other dependencies required
)
